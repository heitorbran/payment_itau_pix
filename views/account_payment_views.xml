<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_account_payment_form_pix" model="ir.ui.view">
            <field name="name">account.payment.form.pix</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='payment_method_line_id']" position="after">
                    <field name="pix_txid" invisible="1"/>
                    <field name="pix_correlation_id" invisible="1"/>
                    <field name="is_pix" invisible="1"/>
                    <field name="pix_installment_id" invisible="1"/>
                    <field name="pix_status" invisible="1"/>
                    <field name="pix_last_sync" invisible="1"/>
                    <field name="pix_raw_response" invisible="1"/>
                </xpath>
                <xpath expr="//header" position="inside">
                    <button name="action_send_pix_itau"
                            string="Enviar PIX Itaú"
                            type="object"
                            class="btn-primary"
                            help="Envia o pagamento via PIX Itaú"
                            invisible="not is_pix or state not in ('posted', 'in_process', 'paid') or pix_txid"/>
                    <button name="action_update_payment_pix_status"
                            string="Atualizar status"
                            type="object"
                            class="btn-secondary"
                            help="Atualiza o status do pagamento PIX Itaú"
                            invisible="not is_pix or not pix_txid"/>
                </xpath>
                <xpath expr="//notebook" position="inside">
                    <page string="PIX Itaú" invisible="not pix_txid">
                        <group>
                            <group>
                                <field name="pix_installment_id" readonly="1"/>
                                <field name="pix_txid" readonly="1"/>
                                <field name="pix_correlation_id" readonly="1"/>
                                <field name="pix_status" readonly="1"/>
                                <field name="pix_last_sync" readonly="1"/>
                            </group>
                        </group>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</odoo>

